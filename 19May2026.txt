use vitcampus;
show tables from vitcampus;

select * from worker;
select first_name, department, salary from worker 
where department='admit' or salary >= 200000;

select worker_id,first_name, department, salary from worker 
where worker_id=1 or worker_id=7;

select worker_id,first_name, department, salary from worker 
where worker_id in (1,3,6);

select worker_id,first_name, department, salary from worker 
where worker_id not in (1,6,7);

select * from worker
where salary between 105000 and 300000;

select * from worker
where salary not between 105000 and 300000;

select * from worker
where salary between 100000 and 500000
and department not in ('hr', 'admin');

select * from worker;

#Like operator
select * from worker where first_name like '__i%';
select * from worker where first_name like 'd%e_';
select * from worker where first_name like '_a__%';
select * from worker where first_name like '_i__a%';

select min(salary) from worker where department = 'admin';
select max(salary) from worker where department = 'HR';
select count(worker_id) from worker where department = 'admin';
select sum(salary) from worker where department = 'admin';
select avg(salary) from worker where department = 'admin';

#HW
select min(salary) from worker 
where department = 'admin'
and first_name not like 'a%';

select salary from worker
union select salary from worker;

select salary from worker
union all select salary from worker;  #dublicate

select * from worker;

select worker_id as rollNum from worker; #as--> temp name

select distinct(department) from worker; #distint--> unique value
#-------------------------------------------------
#conditional statement
select worker_id, first_name,
case
	when salary > 300000 then 'Rich people'
    when salary >= 100000 and salary < 299999 then 'middle'
	else 'poor'
end
as status
from worker;
#-----------------------------------------------
create view minSalaryAdmin as select min(salary) from worker where department = 'admin';
create view MaxSalaryAdmin as select max(salary) from worker where department = 'admin';

select * from minSalaryAdmin;
select * from MaxSalaryAdmin;




